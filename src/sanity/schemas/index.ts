import {postSchema} from './post'
import {authorSchema} from './author'
import {pageSchema} from './page'
import {categorySchema} from './category'

export const schemaTypes = [postSchema, authorSchema, pageSchema, categorySchema]
